<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_main') }}">Home</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_add_quiz') }}">Add Question</a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="{{ path('all_quizz') }}">Modify Quiz</a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="#">History</a>
        </li>

      {% if app.user %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_update_user', {id:app.user.id}) }}">Settings</a>
        </li>
      {% else %}
         <li class="nav-item">
          <a class="nav-link" href="#">Settings</a>
        </li>
      
      {% endif %}

        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_all_user') }}">All User</a>
        </li>
        
      </ul>

    </div>
  </div>
</nav>